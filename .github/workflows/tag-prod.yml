name: tag prod

on:
  workflow_dispatch:
    inputs:
      ref:
        description: |
          Ref to deploy, can be a commit sha or any ref
        default: "dev"
        required: true
        type: string

jobs:
  tag-prod:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref }}
          token: ${{ secrets.MY_GITHUB_ACTION }}

      - name: set tag
        run: |
          git config --local user.name "good bot"
          git config --local user.email good-bot@example.com
          git tag -f prod
          git push -f --tags
